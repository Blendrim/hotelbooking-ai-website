# Public build-time config. VITE_* values are PUBLIC (shipped in the browser bundle).
# The lead form posts SAME-ORIGIN to /api/leads (the Cloudflare Pages Function).
# No secrets here — provider secrets are server-side Cloudflare env vars only.
VITE_API_BASE_URL=/api
